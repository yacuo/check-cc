import { RegionPage } from "@/components/marketing/RegionPage";

export default function ChinaEnglishPage() {
  return <RegionPage href="/en" name="China Mainland" title="What should China Mainland users check before paying for Claude?" description="If your timezone, language, network exit or payment region points to China Mainland, Claude Web, Pro/Max and API access may carry availability or payment risk." />;
}
