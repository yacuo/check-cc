import { RegionPage } from "@/components/marketing/RegionPage";

export default function RussiaEnglishPage() {
  return <RegionPage href="/en" name="Russia" title="What should Russian users check before paying for Claude?" description="Russian users should verify network exit, account region, payment method and API billing availability before subscribing." />;
}
